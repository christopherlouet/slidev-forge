{
  "name": "slidev-forge",
  "version": "1.0.0",
  "description": "Initialisateur de projets Slidev avec themes visuels et configuration YAML",
  "type": "module",
  "bin": {
    "slidev-forge": "./bin/slidev-init.js"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "yaml": "^2.7.0",
    "@inquirer/prompts": "^7.3.2"
  },
  "devDependencies": {
    "vitest": "^3.0.5"
  },
  "engines": {
    "node": ">=20"
  },
  "private": true
}
